{% extends 'base.html.twig' %}



{% block content %}
    <h1>Add a new tag</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    Tag's list:
    <ul>
    {% for tag in tags %}
    <li>{{ tag.name }}</li>

    {% endfor %}
    </ul>
{% endblock %}